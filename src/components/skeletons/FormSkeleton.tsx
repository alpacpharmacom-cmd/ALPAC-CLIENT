/**
 * FormSkeleton — replaced by LoadingSpinner.
 *
 * Form pages (Login, Register, ForgotPassword, ResetPassword, ProductForm)
 * load instantly or use route-level code splitting; they do not need a
 * custom skeleton. Use <LoadingSpinner /> for any transient form loading state.
 *
 * This file is kept to avoid broken imports, but it simply delegates to
 * LoadingSpinner.
 */
export { default } from '../common/LoadingSpinner';
